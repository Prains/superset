# Evidence: remove close buttons from sidebar inline agent dropdown

CDP screenshots from the dev desktop app (worktree spark-seashore, branch remove-agent-close-button) with two Claude agents running in a workspace.

- `agents-card-zoom.png` - agents hover card open, first agent row hovered: no per-agent X, no "Stop all agents" footer.
- `agents-card-full.png` - full app window with the card open for context.
